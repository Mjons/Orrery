---
mtime: 2025-10-04
---

# Phase 0 — scaffold

3–5 days. Boot an empty app with a starfield, wire [[File System Access]]
to pick a folder, and land the [[Glass aesthetic]] so the exit gate
("the shell doesn't look like a generic Vite starter") is obvious at
a glance.

Deliverables landed:

- Vite project, branch `boltzsidian` on the repo.
- Three.js renderer + starfield.
- FS Access handshake with IndexedDB persistence.
- Settings pane scaffold with read-only JSON preview.

Exit gate passed when the first screen looked like something that
belonged to the sim.

Related: [[Phase 1 — read the vault]], [[Vite setup]].

#phase #done
