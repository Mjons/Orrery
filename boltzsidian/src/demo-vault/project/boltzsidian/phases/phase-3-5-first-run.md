---
mtime: 2026-03-02
---

# Phase 3.5 — first run

One week. A new user's first five minutes are crafted — welcome card
with two options (open a folder or try the demo), a curated ~50-note
astronomer's demo vault installed into [[OPFS sandbox]], onboarding
coachmarks that teach gestures one at a time, a tag→kind discovery
modal that fires when the configured mapping doesn't cover the vault,
and an About overlay.

Landed:

- Welcome card + theme picker.
- OPFS install + reset flow.
- Coachmarks with persistent dismissal.
- Tag discovery prompt.
- About pane.
- Canned-template morning report as a [[Dream mode]] preview.

Related: [[First-run experience]], [[Phase 4 — observer chorus]].

#phase #done
